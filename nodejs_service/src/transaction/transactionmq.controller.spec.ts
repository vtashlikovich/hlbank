import { Test, TestingModule } from '@nestjs/testing';
import { TransactionMQController } from './transactionmq.controller';

describe('TransactionMQController', () => {
  let controller: TransactionMQController;

//   beforeEach(async () => {
//     const module: TestingModule = await Test.createTestingModule({
//       controllers: [TransactionMQController],
//     }).compile();

//     controller = module.get<TransactionMQController>(TransactionMQController);
//   });

//   it('should be defined', () => {
//     expect(controller).toBeDefined();
//   });
});
